# SQLite version 3.x
#   gem install sqlite3
development:
  user: dunkstewart
  adapter: postgresql
  database: danger_zone_development
  pool: 5
  timeout: 5000
  min_messages: warning

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  user: dunkstewart
  adapter: postgresql
  database: danger_zone_test
  pool: 5
  timeout: 5000
  min_messages: warning